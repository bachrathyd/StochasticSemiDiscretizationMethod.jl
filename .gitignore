# Machine-local environment / editor state
LocalPreferences.toml
**/LocalPreferences.toml
.claude/
.vscode/
*.code-workspace

# Resolved manifests are not committed for a library (resolve from Project.toml)
Manifest.toml
**/Manifest.toml

# Documenter output
/docs/build/

# Generated figure/image outputs (regenerated by the scripts; final versions
# live in the compiled article under paper/). Data CSVs are kept intentionally.
benchmark/*.png
benchmark/*.pdf
benchmark/*.svg
examples/*.png
examples/*.pdf

# Coverage / profiling / OS cruft
*.jl.cov
*.jl.*.cov
*.jl.mem
coverage/
.DS_Store
Thumbs.db
